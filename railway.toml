[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uv run python -m src.assignment.main --serve --host 0.0.0.0"
healthcheckPath = "/docs"
healthcheckTimeout = 120
restartPolicy = "ON_FAILURE"
